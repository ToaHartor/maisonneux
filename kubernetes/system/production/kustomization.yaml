---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base/system-upgrade
  - ../base/cert-manager
  - ../base/external-secrets
  - ../base/kube-system
  - ../base/nvidia-system # Enable if we use a NVIDIA GPU
  - ../base/opentelemetry
  - ../base/piraeus-datastore
  - ../base/system-utils
  - ../base/monitoring
