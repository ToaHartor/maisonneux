apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: chain-default
spec:
  chain:
    middlewares:
      # - name: secure-headers
      #   namespace: ingress
      - name: default-cors-headers
        namespace: ingress
