apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: chain-local
spec:
  chain:
    middlewares:
      - name: secure-headers
        namespace: ingress
      - name: local-ip
        namespace: ingress
