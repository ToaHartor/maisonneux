---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/refs/heads/main/kustomization-kustomize-v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: mariadb-cluster
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: mariadb-cluster
  dependsOn:
    - name: mariadb-operator
      namespace: operators
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: mariadb-cluster
      namespace: mariadb
  interval: 1h
  path: "./kubernetes/platform/base/mariadb/mariadb-cluster/app"
  postBuild:
    substitute:
      # inherited variables
      fastdata_storage: ${fastdata_storage}
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: mariadb
  timeout: 5m
