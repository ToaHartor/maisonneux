apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: radarr4k-proxy
  namespace: media
spec:
  releaseName: radarr4k-proxy
  dependsOn:
    - name: authentik
      namespace: authentik
  chart:
    spec:
      chart: authentik-application
      # renovate: datasource=docker depName=authentik-application registryUrl=oci://codeberg.org/wrenix/helm-charts
      version: "0.4.9"
      sourceRef:
        kind: HelmRepository
        name: wrenix
        namespace: flux-system
  interval: 50m
  install:
    createNamespace: true
    remediation:
      retries: 3
  values:
    # From https://codeberg.org/wrenix/helm-charts/src/branch/main/authentik-application/values.yaml   
    # Provider is added to the via blueprint in Authentik chart
    blueprint:
      authentik:
        domain: "https://auth.${main_domain_websecure}"
      provider:
        enabled: true
        name: &app-name "Radarr 4K"
        type: "proxy"
        proxy:
          externalHost: &ext-host "https://4kmovies.${main_domain_websecure}"
          skipPathRegex: ""
          cookieDomain: ""
          ingress:
            enabled: false
      application:
        name: *app-name
        slug: "radarr4k"
        # -- put this application in authentik in group
        group: "Media"
        launchURL: *ext-host
        openInNewTab: false
        icon: ""
        description: "4K Movies PVR"
        # publisher: "Publisher"
        bindPolicyID:
        policyEngineMode: "any"
      groups:
        - slug: "app: radarr4k"
          parent: "Media Admins"
          bindID: "c2464fd3-ba4d-47e0-9653-3b22ea1f194b" # arbitrary uuid