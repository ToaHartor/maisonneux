apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  releaseName: cert-manager
  chart:
    spec:
      chart: cert-manager
      version: "1.16.1"
      sourceRef:
        kind: HelmRepository
        name: cert-manager
  interval: 50m
  install:
    remediation:
      retries: 3
  values:
    crds:
      enabled: true
      keep: true
    livenessProbe:
      enabled: true
    # prometheus:
    #   enabled: true