{{ include "common.psqldb" (dict "DatabaseNamespace" .Values.namespaces.postgresNamespace "DatabaseName" .Values.authentik.authentik.postgresql.name "DatabaseOwner" "authentik" "PsqlClusterName" .Values.database.clusterName) }}
