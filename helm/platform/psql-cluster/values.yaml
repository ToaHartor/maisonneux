s3:
  secretName: # bucket-${environment}-s3user
  bucketName: # bucket-${environment}
  endpoint: # http://${minio_url}
  customDomain: # ${minio_url}/bucket-${environment}
  size: 20 # in GB

storage:
  size: 1Gi
database:
  # postgres:
  #   clusterName: # Defined in common
  users:
    - default