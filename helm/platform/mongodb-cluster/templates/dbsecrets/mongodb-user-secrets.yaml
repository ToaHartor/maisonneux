{{- $root := . -}}
{{- range $user := .Values.database.users -}}
---
{{- include "common.secret.creds" (dict "Username" $user "SecretName" (printf "%s-mongodb-secret" $user ) "SecretNamespace" $root.Release.Namespace) }}
{{- end -}}