apiVersion: v2
name: zot
description: Zot Helm chart with resource provisioning
dependencies:
  - name: zot
    # Check version https://github.com/project-zot/helm-charts/releases
    version: &version 0.1.65
    repository: http://zotregistry.dev/helm-charts
    # Other implicit dependencies are installed before this chart by fluxcd :
    # - s3-operator
version: *version
