apiVersion: v2
name: dragonfly-cluster
description: Helm chart provisioning Dragonfly cluster
dependencies:
  - name: common
    version: "0.0.1"
    repository: "file://../../library/common"
    import-values:
      - defaults

# Implicit dependencies : dragonfly-operator
version: 0.0.1
