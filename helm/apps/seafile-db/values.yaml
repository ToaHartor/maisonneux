backupLabel:
  homelab/backup-resource: "true"

seafile:
  database:
    rootSecret: mariadb-root-secret
    user: seafile
    # Database names
    names:
      - seafile_db
      - seahub_db
      - ccnet_db