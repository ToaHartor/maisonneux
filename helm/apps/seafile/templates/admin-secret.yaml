{{- $podEnv := .Values.seafile.workload.main.podSpec.containers.main.env }}
{{- include "common.secret.creds" (dict "Username" "admin" "SecretName" $podEnv.INIT_SEAFILE_ADMIN_EMAIL.secretKeyRef.name "SecretNamespace" .Release.Namespace) }}