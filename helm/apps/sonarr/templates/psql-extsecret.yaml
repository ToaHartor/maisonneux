{{- $secretName := include "common.db.secret-name" (dict "DatabaseUser" .Values.sonarr.workload.main.podSpec.containers.main.env.SONARR__POSTGRES__USER )}}
{{- include "common.db.extsecret" (dict "Namespace" .Release.Namespace "SecretName" $secretName "ClusterSecretName" .Values.database.postgres.clusterSecretName ) }}