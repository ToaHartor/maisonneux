apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cyberchef-proxy
  namespace: services
spec:
  releaseName: cyberchef-proxy
  dependsOn:
    - name: authentik
      namespace: authentik
  chart:
    spec:
      chart: authentik-application
      # renovate: datasource=docker depName=authentik-application registryUrl=oci://codeberg.org/wrenix/helm-charts
      version: "0.4.9"
      sourceRef:
        kind: HelmRepository
        name: wrenix
        namespace: flux-system
  interval: 50m
  install:
    createNamespace: true
    remediation:
      retries: 3
  values:
    # From https://codeberg.org/wrenix/helm-charts/src/branch/main/authentik-application/values.yaml   
    # Provider is added to the via blueprint in Authentik chart
    blueprint:
      authentik:
        domain: "https://auth.${main_domain_websecure}"
      provider:
        enabled: true
        name: "Cyberchef"
        type: "proxy"
        proxy:
          externalHost: "https://cyberchef.${main_domain_websecure}"
          skipPathRegex: ""
          cookieDomain: ""
          ingress:
            enabled: false
      application:
        name: "Cyberchef"
        slug: "cyberchef"
        # -- put this application in authentik in group
        group: ""
        launchURL: "https://cyberchef.${main_domain_websecure}"
        openInNewTab: false
        icon: ""
        description: "Cryptographic utilities"
        # publisher: "Publisher"
        bindPolicyID:
        policyEngineMode: "any"
      groups: []